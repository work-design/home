<%= form_with theme: 'weui_search', url: my_requirements_path, model: Requirement.new do |f| %>
  <%= f.text_field :name %>
  <%= f.submit %>
  <%= link_to t('.clear'), filter_params(except: [:name]), class: 'ui button' %>
<% end %>
